# 2026-08-31 工作日志

## 前端科技化重构（UI/UX 完整重构）✅ 已完成

- **范围**：仅前端表现层，业务逻辑零破坏（API/后端/数据库/Migration/Docker 均未动）
- **提交**：
  - `f672296` checkpoint（重构前快照）
  - `3180830` 重构主提交（9 files, +1333/−123）
  - `fe41e69` 重构报告
- **核心成果**：
  - Design Token 体系（style.css `:root`，深蓝 #020814 + 主蓝 #008CFF + 青 #00E5FF）
  - 新增 `icons.ts` + `TechIcon.vue` 统一 SVG 图标库（替换 Unicode 字符）
  - 新增 `frontend/public/favicon.svg`（修复 404）
  - App.vue 重写：Header 实时时钟/全屏/WS 状态、`dialogConfirm`（替换原生 confirm/prompt，含 input 模式）、CameraPanel HUD 取景框、RTSP 脱敏
  - BatchCameraModal / EvidencePreview 深色化
  - 响应式 1920/1600/1440/1366，消除横向滚动（原 body min-width:1180px 已移除）
- **验收**：vue-tsc 通过；vite build 通过；playwright-core 自动化 7 项滚动检测 OK、弹窗 2/2、pageErrors 0；截图 9 张存于 `data/ui_shots/`（模型无读图能力，需人工复核）

## 已知问题（记录，未处理）

1. **dev SQLite 库 Alembic revision mismatch**：`data/yolo_vlm.db` 的 alembic_version 指向不存在的 `20260828_04`。按约定未改数据库；用全新临时库 `data/ui_test_tmp.db` 测试（已删）。后续需有权限者核对迁移链或改用 PostgreSQL。
2. Vite ws 代理偶发 503/ECONNRESET（瞬时，非 UI 引入）。

## 环境备忘

- 本机无 Docker Desktop 时可用 `.venv/Scripts/python.exe run.py` 直启后端（DATABASE_URL=sqlite:///... SCHEDULER_ENABLED=false）
- playwright-core 装在 `C:/Users/Administrator/.workbuddy/binaries/node/workspace`；Chrome 路径 `chrome-152.0.7977.64/chrome.exe`
- 测试摄像头创建需满足几何约束：off_duty 需 post_roi≥3 点、people_flow 需 flow_line 恰 2 点、intrusion 需禁区≥3 点（否则 422）
