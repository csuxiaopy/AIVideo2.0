- 将 compose.cpu.yml 的 app 服务改为代码挂载（backend/alembic/run.py/frontend/dist 绑定映射到 /app），改代码无需重 build 镜像，compose config 校验通过。
- run.py/config.py 增加 APP_RELOAD 设置（uvicorn 热重载，reload_dirs 限定 backend/），compose.cpu.yml 默认开启 APP_RELOAD=true，配合代码挂载实现改码即生效。
- compose.cpu.yml 新增 web 构建服务（node:22-alpine + pnpm + vite build --watch），自动构建前端到 ./frontend/dist 供 app 挂载，node_modules 用 web_node_modules 卷隔离防止覆盖宿主机 Windows 二进制。
- Docker 部署配置全面体检：发现 .dockerignore 缺失（历史权限报错根因）、Dockerfile 用 npm 与 pnpm 锁文件不匹配、compose web 服务镜像源不一致、package.json 缺 packageManager 字段。已全部修正：新建 .dockerignore、Dockerfile 改 corepack+pnpm --frozen-lockfile+npmmirror、compose web 用 daocloud 源、package.json 加 pnpm@10.12.1。
- 用户明确「不要使用 SQLite」：发现 HEAD（d0135b5）仍为 SQLite 版（此前 PostgreSQL 改动被 git 还原丢失）。重新改回 PostgreSQL 并提交 5b33030：compose.cpu.yml 恢复 postgres:17-alpine 服务（postgres_data 卷、pg_isready 健康检查、POSTGRES_PASSWORD 强校验 :? 语法、app 恢复 DATABASE_URL=postgresql+psycopg 与 evidence_data/snapshot_data 卷、security_opt 保留并注释新环境可删）；.env.example 与 alembic.ini 连接串改 postgres；清理 backend/repository.py 未使用的 sqlite 方言 import（upsert_traffic 为 select-then-update 通用写法，跨库兼容）；docs/CPU_DOCKER_OPS_GUIDE.md 重写对齐（docker compose v2、pnpm/hoisted/daocloud、dev 覆盖层、seccomp 兼容说明、pg_dump 备份）。注意：POSTGRES_PASSWORD 用 ${VAR:?} 强校验，config 校验需先 cp .env.example .env（.gitignore 已忽略 .env）。backend/config.py 与 database.py 的 sqlite 分支保留（本机裸跑开发兜底，生产不命中）。
- 用户背景补充：目标服务器「没有运行中的容器，只有已构建的镜像」→ 部署计划应复用现有镜像 + PostgreSQL。
- README.md 新增「检测模型 / 执行流程 / 警告判断逻辑」三节（提交 545ec07）：梳理 yolo26n(COCO)+fire_smoke_yolov8(SHA256校验)+VLM分级+统计黑屏四类模型；调度错峰、Redis优先级双队列（fire独立critical）、模式节流、证据保留30天；八种模式逐条触发条件与告警行为（含黑屏连续3帧、烟火火2帧/烟5帧中3帧、行为60秒2窗口、闯入绕过冷却等），并把开头「六种模式」更正为八种。
- 实现告警中心证据图片点击放大查看：新增 frontend/src/components/EvidencePreview.vue（80x50 缩略图 + Teleport 到 body 的 Lightbox，支持滚轮缩放 50%-500%、拖拽平移、缩放比例显示、ESC/遮罩/X 三种关闭、body 滚动锁定与恢复、图片加载失败降级"暂无证据"），App.vue 告警表格证据列替换为该组件。vue-tsc 类型检查与 vite 构建通过，dist 已重建。字段：evidence_url（后端拼 /evidence/{文件名}）。
- 安全修复（main.py spa 兜底路由）：原 _mount_frontend 的 catch-all 路由存在目录穿越漏洞，GET /evidence/../../../alembic.ini 可读取项目根任意文件（实测复现，泄露数据库连接串）。修复为 resolve()+relative_to(root) 目录包含校验，越界请求回退 index.html。实测穿越攻击全部被拦截，正常证据图/静态资源/SPA 路由不受影响。/evidence/{filename} 端点本身已有 split("/")[-1] 净化，无需改动。
- 注意：沙箱内 taskkill //PID 语法会失败且不生效，需用 /PID 格式并在沙箱外执行；沙箱内 vite build 写入的文件在沙箱外不可见（overlay），构建产物替换需用非沙箱命令。
- 实现告警清理功能（未提交git）：models.py 新增 RetentionSettings 单行表（alert_retention_days=30/auto_cleanup_enabled/updated_at）；alembic/versions/20260828_03_alert_retention.py（revision=20260828_03, down=20260826_02，sa.inspect 判表存在再 create_table，downgrade 留空）；repository.py 新增 get/save_retention_settings（照搬 webhook get-or-create id=1 模式）+ list_alerts_before/delete_alerts_before（均支持可选 severity）；新建 backend/cleanup.py CleanupService.run(override_days, severity)——resolve()+relative_to() 越界防护删证据文件（单条容错 logger.warning）→ 再删 DB 记录，返回 deleted/evidence_removed/cutoff/days；pipeline.py 挂载 _cleanup_loop 6小时定时（先 sleep 再跑、auto_cleanup_enabled 开关、logger.exception，cleanup_task 并入 close cancel+gather）；schemas.py RetentionSettingsUpdate（1-365 天）；API：GET/PUT /api/settings/retention（settings.py）、DELETE /api/alerts?before_days&severity（monitoring.py，asyncio.to_thread 调 cleanup.run，422 校验）；前端 App.vue：settings 页新增「数据保留」panel（天数 input+switch+保存）+ 告警中心「清理」按钮（prompt 输天数+confirm+notify）；tests/test_cleanup.py 6 个用例全过（到期删/未到期留/override_days/severity 过滤/越界不删文件/缺文件容错）。验证：全量 pytest 33 passed、vue-tsc 通过、vite build 重建 dist、alembic upgrade head 空库成功建表、TestClient 冒烟（GET/PUT retention、DELETE alerts、422 边界）全通过、compose 两组 config 校验通过。
